/**
 * Userscripts must default-export an async function that takes this object as a sole argument, e.g. export default
 * async function (util) Note that commonly it is done by using destructing syntax.
 *
 * @typedef {import("../common/typedef.js").Utils<import("./Addon.js")>} PopupUtils
 */

export default {};
